//class for setting width and height of the window, as the game window should always be a square

public class Const {
    private static final int SIDE_LENGTH = 900;
    public static final int WIDTH = SIDE_LENGTH;
    public static final int HEIGHT = SIDE_LENGTH;
}